# ========================================
# RAILWAY ENVIRONMENT VARIABLES
# ========================================
# Copy-paste these into Railway Dashboard → Settings → Variables
# Note: Railway will automatically provide DATABASE_URL and REDIS_URL if you add those services

# --- Node Environment ---
NODE_ENV=production
PORT=3000

# --- Virtuals Protocol (ACP) ---
# Required for Virtuals Protocol integration
# Get from: https://agdp.io/ (Agent Development Portal)
VIRTUALS_ENTITY_ID=your_entity_id_here
WALLET_PRIVATE_KEY=your_private_key_here

# --- RentAHuman ---
# Optional - uses public API if not provided
# RENTAHUMAN_API_KEY=your_api_key_here

# --- Railway Services (Auto-configured) ---
# Railway automatically provides these when you add PostgreSQL/Redis services:
# DATABASE_URL=<auto-configured by Railway>
# REDIS_URL=<auto-configured by Railway>

# ========================================
# INSTRUCTIONS:
# ========================================
# 1. Go to Railway Dashboard → Your Project → Settings → Variables
# 2. Click "New Variable" for each variable below
# 3. Replace placeholder values with your actual credentials
# 4. DO NOT commit this file with real values to GitHub (.env is gitignored)
